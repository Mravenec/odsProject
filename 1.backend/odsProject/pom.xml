<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.14</version>
		<relativePath/>
	</parent>
	<groupId>com.odsProject</groupId>
	<artifactId>odsProject</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>odsProject</name>
	<description>Sistema ODS Agenda 2030 — UTN Costa Rica</description>
	<properties>
		<java.version>21</java.version>
		<jooq.version>3.19.6</jooq.version>
	</properties>

	<dependencies>
		<!-- Web MVC -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- JOOQ -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jooq</artifactId>
		</dependency>

		<!-- MariaDB Driver -->
		<dependency>
			<groupId>org.mariadb.jdbc</groupId>
			<artifactId>mariadb-java-client</artifactId>
			<version>3.3.3</version>
		</dependency>

		<!-- Spring Security -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<!-- JWT -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.5</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.5</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.5</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Formula engine (fórmulas variables) -->
		<dependency>
			<groupId>net.objecthunter</groupId>
			<artifactId>exp4j</artifactId>
			<version>0.4.8</version>
		</dependency>

		<!-- Excel export (ExportService) -->
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>5.2.5</version>
		</dependency>

		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- Tests -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<!-- Evita que el IDE (JDT/m2e) ejecute JOOQ en cada import; usa fuentes ya generadas -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>org.jooq</groupId>
										<artifactId>jooq-codegen-maven</artifactId>
										<versionRange>[3.0,)</versionRange>
										<goals>
											<goal>generate</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore />
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- JOOQ Code Generation Plugin -->
			<plugin>
				<groupId>org.jooq</groupId>
				<artifactId>jooq-codegen-maven</artifactId>
				<version>${jooq.version}</version>
				<configuration>
					<jdbc>
						<driver>org.mariadb.jdbc.Driver</driver>
						<url>jdbc:mariadb://localhost:3306/</url>
						<user>root</user>
						<password>123456</password>
					</jdbc>
					<generator>
						<database>
							<name>org.jooq.meta.mariadb.MariaDBDatabase</name>
							<includes>.*</includes>
							<excludes>
								<!-- Permitir vistas para reportes administrativos -->
							</excludes>
							<schemata>
								<schema><inputSchema>ods_login</inputSchema></schema>
								<schema><inputSchema>ods_master</inputSchema></schema>
								<schema><inputSchema>ods01</inputSchema></schema>
								<schema><inputSchema>ods02</inputSchema></schema>
								<schema><inputSchema>ods03</inputSchema></schema>
								<schema><inputSchema>ods04</inputSchema></schema>
								<schema><inputSchema>ods05</inputSchema></schema>
								<schema><inputSchema>ods06</inputSchema></schema>
								<schema><inputSchema>ods07</inputSchema></schema>
								<schema><inputSchema>ods08</inputSchema></schema>
								<schema><inputSchema>ods09</inputSchema></schema>
								<schema><inputSchema>ods10</inputSchema></schema>
								<schema><inputSchema>ods11</inputSchema></schema>
								<schema><inputSchema>ods12</inputSchema></schema>
								<schema><inputSchema>ods13</inputSchema></schema>
								<schema><inputSchema>ods14</inputSchema></schema>
								<schema><inputSchema>ods15</inputSchema></schema>
								<schema><inputSchema>ods16</inputSchema></schema>
								<schema><inputSchema>ods17</inputSchema></schema>
							</schemata>
						</database>
						<target>
							<packageName>com.odsProject.odsProject.database.jooq</packageName>
							<directory>src/main/java</directory>
						</target>
						<generate>
							<pojos>true</pojos>
							<immutablePojos>false</immutablePojos>
							<daos>false</daos>
							<fluentSetters>true</fluentSetters>
							<implicitJoinPathsToOne>false</implicitJoinPathsToOne>
							<implicitJoinPathsToMany>false</implicitJoinPathsToMany>
						</generate>
					</generator>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<!-- Regenerar JOOQ solo cuando MariaDB está disponible: mvn -Pjooq-codegen generate-sources -->
	<profiles>
		<profile>
			<id>jooq-codegen</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jooq</groupId>
						<artifactId>jooq-codegen-maven</artifactId>
						<version>${jooq.version}</version>
						<executions>
							<execution>
								<id>generate-jooq-all</id>
								<phase>generate-sources</phase>
								<goals>
									<goal>generate</goal>
								</goals>
							</execution>
						</executions>
						<dependencies>
							<dependency>
								<groupId>org.mariadb.jdbc</groupId>
								<artifactId>mariadb-java-client</artifactId>
								<version>3.3.3</version>
							</dependency>
						</dependencies>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
